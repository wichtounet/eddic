/* 
 * Standard Library : Arrays functions
*/

//Sum the values of the array
int sum(int[] array){
    int sum = 0;

    foreach(int value in array){
        sum = sum + value;
    }

    return sum;
}
