struct Complex {
    int imag;
    int real;
}

void main(){
    println("Structures test");

    Complex c;
    c.imag = 222;
    c.real = 666;

    println(c.imag);
    println(c.real);

    c.imag += 111;
    c.real += 222;

    println(c.imag);
    println(c.real);
}
